# Spec: 赚了要不要走？——止盈规则

> 所有命令在沙箱外运行。

## 上下文

在 `q4-when-to-trade.ipynb` 中已有：
- 不同调仓频率的对比结果
- 止损规则在不同阈值下的对比结果
- 沿用 spec-01 的固定数据窗口：`START = "2021-01-01"`，`END = "2026-03-18"`，不要重新引入 `today` / `now`
- 沿用 spec-01 的 `FEE_MODEL` 和 `run_backtest(frequency, fee_model=None, order_rules=None)` 辅助函数

当前问题：止损规则已经测过了。反过来——赚了一定程度，要不要主动卖掉、落袋为安？

## 任务

在 notebook 中新建代码单元格，给策略加上止盈规则，对比不同止盈阈值的效果。

## 要求

1. 阅读以下 oxq 模块的源码，了解接口的输入、输出和参数含义：
   - `oxq.rules.TakeProfitRule`（注意：TakeProfitRule 作为 rule 传入 `Engine.run(rules=[...])`，不再是 Strategy 字段）

2. 解释 TakeProfitRule 的工作原理：
   - 当持有仓位时，TakeProfitRule 会以 `avg_cost x (1 + threshold)` 为止盈价，提交一个 limit SELL 挂单给 SimBroker
   - SimBroker 在后续每个 bar 检查：如果收盘价涨到止盈价以上，自动触发卖出
   - 和止损一样是挂单机制——"预设一个目标，到了就自动锁定利润"

3. 从 spec-02 的回测结果中，**用代码动态选出**夏普比率最高的止损阈值，与 spec-01 选出的最优频率一起作为基准（不要硬编码数字）：
   ```python
   BEST_SL = max([sl for sl in STOP_THRESHOLDS if sl is not None],
                 key=lambda sl: results_sl[sl].sharpe_ratio())
   ```

4. 定义 4 组对比：不止盈 / threshold=0.10 / 0.20 / 0.30

5. 全部含成本回测。调用 spec-01 定义的 `run_backtest` 辅助函数，将止损和止盈规则作为 order_rules 传入：
   ```python
   run_backtest(
       frequency=BEST_FREQ,
       fee_model=PercentageFee(rate=Decimal("0.001"), min_fee=Decimal("5")),
       order_rules=[StopLossRule(threshold=BEST_SL), TakeProfitRule(threshold=TP)],
   )
   ```
   在 `run_backtest` 内部，所有规则与 `RebalanceFrequencyRule` 一起传入 `Engine.run(rules=[...])`。

6. 打印对比表：止盈阈值、累计收益率、年化波动率、最大回撤、夏普比率、交易次数、总手续费

7. 画净值曲线对比图（figsize 12x6）：
   - 不止盈（灰色虚线）vs 3 种止盈阈值（不同颜色实线）
   - 归一化到起点 = 100
   - 标题「止盈规则对比（含交易成本）」

8. 打印交易记录（取止盈阈值最小的那组，展示前 10 笔），把订单的 `order_type` 字段一起打出来——让学员看到回测里订单类型这一层的存在（至于前 10 笔具体出现几种类型，取决于挂单是否被触发，不预设）。

9. 打印分析（根据实际数据动态描述方向，不要预设结果方向）：
   - 止盈对累计收益率和夏普比率的影响——逐个阈值看，分别是抬高、压低，还是基本不变？
   - 不同阈值之间的差异——是单调变化（越紧越差或越松越好），还是非单调（中间某个阈值反向）？
   - 止盈触发的额外交易次数和手续费变化
   - 与上一节止损结果的对照——两类规则在你这段数据上分别压低/抬高了哪些指标，方向是否一致
   - 「交易圈有句老话：'让利润奔跑，截断亏损'（Let profits run, cut losses short）。这是经验总结，不是定理——它在你这段数据上成不成立，看实验数据怎么说。」
   - 「加规则之前，先问自己：这条规则的逻辑站得住脚吗？换个阈值、换段数据，结论还一样吗？」

10. 固定窗口参考结果（`START = "2021-01-01"`，`END = "2026-03-18"`，基准为 10 天调仓 + 5% 止损），用于判断是否复现书稿：
    - 不止盈：`100.78% / 10.71% / -13.25% / 1.36 / 389 / 3903`
    - 10% 止盈：`72.73% / 9.70% / -9.47% / 1.18 / 431 / 6703`
    - 20% 止盈：`83.48% / 10.04% / -13.25% / 1.26 / 409 / 5215`
    - 30% 止盈：`104.32% / 10.23% / -13.25% / 1.45 / 396 / 4306`
    - 这组数据里 10% / 20% 止盈降低收益，30% 止盈提高收益和简化夏普；分析不要写成"三种止盈阈值都降低收益"

## 结果呈现

1. TakeProfitRule 工作原理说明
2. 指标对比表
3. 净值曲线对比图
4. 交易记录（前 10 笔）
5. 分析文字

## 验证

执行成功的标志：
- 新增的单元格无报错运行完毕
- 对比表有 4 行，数值合理
- 净值曲线图中不止盈为灰色虚线，有止盈为彩色实线
- 交易记录前 10 笔有 `order_type` 列，能看到订单类型字段（具体是 market、stop 还是 limit，取决于回测过程中是否触发挂单——若前 10 笔都是 market，说明这一段时段内挂单未被命中，这本身就是一个值得解读的现象）
- 参考结果与上面的固定窗口数值一致，或差异小到只来自格式化四舍五入
